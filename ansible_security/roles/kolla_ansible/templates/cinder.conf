{% raw %}
[DEFAULT]
# when enabled ceph, use the same hostname for cinder-volume and cinder-backup
# for ha
{% if enable_ceph | bool and service_name in ['cinder-volume', 'cinder-backup'] %}
host = control
{% endif %}

[rbd-1]
image_upload_use_cinder_backend = False
{% endraw %}
